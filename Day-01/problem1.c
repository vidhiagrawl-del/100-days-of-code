#include <stdio.h>

int main() 
{
    int a, b, sum;

    // Input two numbers
    scanf("%d %d", &a, &b);

    // Calculate sum
    sum = a + b;

    // Display result
    printf("Sum = %d", sum);

    return 0;
}